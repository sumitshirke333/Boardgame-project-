pipeline {
    agent any

    environment {
        // 🌍 App Details
        IMAGE_NAME = 'boardgame'
        WORKER_IP = '35.154.248.228'       // 👈 Replace with your Worker Node's Public IP

        // 🔐 Docker Hub
        DOCKERHUB_USER = 'sumitshirke369' // 👈 Your Docker Hub username

        // 🔐 SonarQube
        SONARQUBE_ENV = 'SonarQube'       // Must match name in Jenkins > Manage Jenkins > System
    }

    tools {
        maven 'Maven'
    }

    stages {

        // 1️⃣ Checkout Code from GitHub
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/sumitshirke333/Boardgame-project-.git',
                    credentialsId: 'github-credentials'
            }
        }

        // 2️⃣ Run Unit Tests
        stage('Run Tests') {
            steps {
                sh 'mvn clean test'
            }
            post {
                unsuccessful {
                    error("❌ Tests failed! Stopping the pipeline.")
                }
            }
        }

        // 3️⃣ SonarQube Code Analysis
        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv("${SONARQUBE_ENV}") {
                    sh 'mvn verify sonar:sonar'
                }
            }
        }

        // 4️⃣ Build Maven Project
        stage('Build Maven Project') {
            steps {
                sh 'mvn clean package -DskipTests'
            }
        }

        // 5️⃣ Build Docker Image
        stage('Build Docker Image') {
            steps {
                sh '''
                docker build -t $DOCKERHUB_USER/$IMAGE_NAME:latest .
                docker images
                '''
            }
        }

        // 6️⃣ Trivy Image Scan (With Custom Temp Directory)
        stage('Trivy Scan') {
            steps {
                withEnv(["TRIVY_TMP_DIR=/var/lib/trivy"]) {
                    sh '''
                    mkdir -p $TRIVY_TMP_DIR
                    trivy image --exit-code 0 --severity HIGH,CRITICAL $DOCKERHUB_USER/$IMAGE_NAME:latest
                    '''
                }
            }
        }

        // 7️⃣ Push Docker Image to Docker Hub
        stage('Push to Docker Hub') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    sh '''
                        echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin
                        docker push $DOCKER_USER/$IMAGE_NAME:latest
                    '''
                }
            }
        }

        // 8️⃣ Deploy to Worker Node via SSH + Docker
        stage('Deploy to Worker Node') {
            steps {
                sshagent(['worker-ssh']) {
                    sh '''
                        ssh -o StrictHostKeyChecking=no ubuntu@$WORKER_IP "
                            docker pull $DOCKERHUB_USER/$IMAGE_NAME:latest &&
                            docker stop boardgame || true &&
                            docker rm boardgame || true &&
                            docker run -d --name boardgame -p 8080:8080 $DOCKERHUB_USER/$IMAGE_NAME:latest
                        "
                    '''
                }
            }
        }
    }

    post {
        success {
            echo '✅ Pipeline completed successfully! 🚀'
        }
        failure {
            echo '❌ Pipeline failed. Check logs above for details.'
        }
    }
}
